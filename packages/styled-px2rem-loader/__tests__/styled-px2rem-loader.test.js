'use strict';

const styledPx2remLoader = require('..');
const assert = require('assert').strict;

assert.strictEqual(styledPx2remLoader(), 'Hello from styledPx2remLoader');
console.info('styledPx2remLoader tests passed');
